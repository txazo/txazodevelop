缓存

1. 缓存：读写比很高、读取频繁、很少变化的数据

2. 缓存读取数据
   1) 命中：从缓存读取
   2) 未命中：从数据库读取，写入缓存
   
3. 缓存策略
   1) 实时更新缓存：数据更新时立即更新缓存
   2) 非实时更新缓存：数据更新时不会更新缓存，设置失效时间
   
4. 分布式缓存架构
   1) Memcached，Redis
   2) Memcached：集中式，各节点互不通信，应用通过一致性Hash算法选择缓存服务器